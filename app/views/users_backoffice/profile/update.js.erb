$('#myModal').modal('hide');

document.getElementById('profileAvatarSm').src = "<%= url_for(@user.user_profile.avatar) %>"
document.getElementById('profileAvatarLg').src = "<%= url_for(@user.user_profile.avatar) %>"
document.getElementById('modalAvatarImg').src = "<%= url_for(@user.user_profile.avatar) %>"

$.bootstrapGrowl('Avatar atualizado com sucesso!', {
  type: 'success',
  align: 'right',
  allow_dismiss: true,
  stackup_spacing: 10
});